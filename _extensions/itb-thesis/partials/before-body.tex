$if(has-frontmatter)$
\frontmatter
$endif$

% Diletakkan setelah \begin{document} agar menang atas lokalisasi babel/Quarto.
\renewcommand{\chaptername}{BAB}
\renewcommand{\contentsname}{DAFTAR ISI}
\renewcommand{\listfigurename}{DAFTAR GAMBAR}
\renewcommand{\listtablename}{DAFTAR TABEL}
\renewcommand{\bibname}{DAFTAR PUSTAKA}

% Sampul
\begin{titlepage}
\pagestyle{empty}
\begin{center}
  \Large\bfseries\MakeUppercase{$title$}

  \vfill
  \Large $if(document-type)$$document-type$$else$Laporan Tugas Akhir$endif$

  \vfill
  \large $if(degree-statement)$$degree-statement$$else$Disusun sebagai salah satu syarat untuk memperoleh gelar$endif$

  $if(degree)$\large $degree$$endif$

  \vfill
  \large Oleh

  \medskip
  \Large $for(authors)$$it.name.literal$$sep$, $endfor$

  $if(student-id)$\large NIM $student-id$$endif$

  \vfill
  $if(cover-logo)$
  \includegraphics[width=0.15\textwidth]{$cover-logo$}
  $else$
  \includegraphics[width=0.15\textwidth]{cover-ganesha.jpg}
  $endif$

  \vfill
  \large\MakeUppercase{
    $if(study-program)$$study-program$\\$endif$
    $if(school)$$school$\\$endif$
    $if(university)$$university$$else$Institut Teknologi Bandung$endif$
  }

  \medskip
  \large $if(cover-date)$$cover-date$$else$$date$$endif$
\end{center}
\end{titlepage}
\clearpage

% Lembar persetujuan
\thispagestyle{empty}
\begin{center}
  \Large\bfseries\MakeUppercase{$title$}

  \vfill
  \Large $if(document-type)$$document-type$$else$Laporan Tugas Akhir$endif$

  \vfill
  \large Oleh

  \medskip
  \Large $for(authors)$$it.name.literal$$sep$, $endfor$

  $if(student-id)$\large NIM $student-id$$endif$

  \medskip
  \large
  $if(study-program)$$study-program$\\$endif$
  $if(school)$$school$\\$endif$
  $if(university)$$university$$else$Institut Teknologi Bandung$endif$

  \vfill
  \normalsize\normalfont
  Telah disetujui dan disahkan di
  $if(city)$$city$$else$Bandung$endif$$if(approval-date)$, pada tanggal $approval-date$$endif$.

  \vfill
  $if(supervisors)$
  $for(supervisors)$
  \begin{minipage}[t]{0.45\textwidth}
    \centering
    $if(it.role)$$it.role$$else$Pembimbing$endif$\\[25mm]
    $it.name$\\
    $if(it.nip)$NIP $it.nip$$endif$
  \end{minipage}
  $sep$\hfill$endfor$
  $else$
  Pembimbing\\[25mm]
  Nama dan Gelar Pembimbing
  $endif$
\end{center}
\clearpage

% Lembar pernyataan keaslian
\chapter*{LEMBAR PERNYATAAN}
\thispagestyle{empty}

Dengan ini saya menyatakan bahwa:

\begin{enumerate}
  \item Pengerjaan dan penulisan dokumen ini dilakukan tanpa menggunakan bantuan yang tidak dibenarkan.
  \item Segala bentuk kutipan dan acuan terhadap tulisan orang lain telah dituliskan dengan baik dan benar.
  \item Dokumen ini belum pernah diajukan pada program pendidikan di perguruan tinggi mana pun.
\end{enumerate}

$if(statement-text)$$statement-text$$endif$

\vspace{15mm}
$if(city)$$city$$else$Bandung$endif$$if(statement-date)$, $statement-date$$endif$\\
$for(authors)$$it.name.literal$$sep$, $endfor$\\
$if(student-id)$NIM $student-id$$endif$
\clearpage

$if(abstract-id)$
\chapter*{ABSTRAK}
\addcontentsline{toc}{chapter}{Abstrak}
$abstract-id$

$if(keywords-id)$
\medskip
\noindent\textbf{Kata kunci:} $for(keywords-id)$$it$$sep$, $endfor$.
$endif$
\clearpage
$endif$

$if(abstract-en)$
\chapter*{ABSTRACT}
\addcontentsline{toc}{chapter}{Abstract}
$abstract-en$

$if(keywords-en)$
\medskip
\noindent\textbf{Keywords:} $for(keywords-en)$$it$$sep$, $endfor$.
$endif$
\clearpage
$endif$

$if(acknowledgements)$
\chapter*{KATA PENGANTAR}
\addcontentsline{toc}{chapter}{Kata Pengantar}
$acknowledgements$
\clearpage
$endif$

\pagestyle{plain}
